# Environment variables for Next.js Portfolio
# Copy this file to .env.local and fill in the secrets.
# NEVER commit .env.local with real credentials.

# Email address that will receive contact form submissions (DO NOT put private inbox if you don't want it public)
# Use a placeholder here; set the real one in your untracked .env.local
CONTACT_TO=your_public_contact_email@example.com

# Gmail (or other SMTP) credentials used to send the email
# For Gmail: enable 2FA, then create an App Password (16 chars no spaces) and paste below.
SMTP_USER=your_gmail_address@gmail.com
SMTP_PASS=your_app_password_here

# Optional: override the site URL (useful for absolute URLs in SEO/meta tags)
# NEXT_PUBLIC_SITE_URL=https://your-domain.com

# Add any future public vars with NEXT_PUBLIC_ prefix, e.g.:
# NEXT_PUBLIC_ENABLE_ANIMATIONS=true
